
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.minargo.foundation',
  appName: 'Minar Go Foundation',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
